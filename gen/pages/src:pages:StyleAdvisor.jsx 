import React from 'react';
import { motion } from 'framer-motion';
import { Button } from '@/components/ui/button';
import { toast } from '@/components/ui/use-toast';
import { Wand2 } from 'lucide-react';

const StyleAdvisor = () => {
  const handleFeatureClick = (feature) => {
    toast({
      title: "🚧 This feature isn't implemented yet—but don't worry! You can request it in your next prompt! 🚀"
    });
  };

  return (
    <div className="min-h-screen bg-white">
      <section className="relative h-96 bg-gray-100 flex items-center justify-center">
        <div className="text-center px-4">
          <motion.h1
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            className="text-6xl md:text-8xl"
          >
            Style Advisor
          </motion.h1>
          <motion.p
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6, delay: 0.1 }}
            className="text-gray-500 text-lg mt-4 max-w-2xl mx-auto"
          >
            Your personal AI-powered stylist. Get outfit recommendations tailored to your taste, the occasion, and the weather.
          </motion.p>
        </div>
      </section>

      <section className="section-padding">
        <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8 }}
          >
            <Wand2 className="h-20 w-20 text-black mx-auto mb-8" />
            <h2 className="text-5xl mb-6">Coming Soon</h2>
            <p className="text-gray-600 text-lg leading-relaxed mb-8">
              We're putting the finishing touches on our revolutionary Style Advisor. This feature will analyze your preferences and our collection to create perfect looks, just for you.
            </p>
            <Button size="lg" className="px-10 py-6 text-base" onClick={() => handleFeatureClick('Style Advisor')}>
              Get Notified When It's Ready
            </Button>
          </motion.div>
        </div>
      </section>
    </div>
  );
};

export default StyleAdvisor;